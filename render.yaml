services:
  - type: web
    name: devtrack-combined
    env: docker
    dockerContext: .
    dockerfilePath: Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: DatabaseProvider
        value: InMemory
      - key: ApiSettings__BaseUrl
        value: http://localhost:5000
